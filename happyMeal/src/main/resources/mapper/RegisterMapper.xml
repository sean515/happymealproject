<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.meal.happy.dao.RegisterDAO">
	<select id="loginOk" resultType="com.meal.happy.dto.RegisterDTO">
		select userid, username from register
		where userid = #{param1} and userpwd = #{param2}
	</select>
	<select id="idCheckCount" resultType="int">
		select count(userid) cnt from register where userid=#{param1}
	</select>
	<insert id="registerInsert">
		insert into register(userid, userpwd, username, email, hobby) 
		values (#{userid}, #{userpwd}, #{username}, #{email}, #{hobby})
	</insert>
	<select id="registerEdit" resultType="com.meal.happy.dto.RegisterDTO">
		select userid, username, email, hobby from register 
		where userid=#{param1}
	</select>
	<update id="registerEditOk">
		update register set email=#{email}, hobby=#{hobby} where userid=#{userid} and userpwd=#{userpwd}
	</update>
	<select id="idSearch" resultType="string">
		select rpad(substr(userid,1,length(userid)/2),
		length(userid),'*') userid from register 
		where username=#{param1} and email=#{param2}
	</select>
</mapper>